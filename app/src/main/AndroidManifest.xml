<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="de.jerleo.android"
    android:versionCode="1"
    android:versionName="1.0">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MeterActivity" />
        <activity android:name=".ReadingList" />
        <activity android:name=".ReadingActivity" />
        <activity android:name=".TariffList" />
        <activity android:name=".TariffActivity" />
        <activity android:name=".BillActivity" />
        <activity android:name=".ReadingChart" />

    </application>

</manifest>